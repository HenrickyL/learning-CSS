import {styled} from 'styled-components'

export const ContainerWrapper  = styled.div`
    position: relative;
    display: flex;
    justify-content: center;
    align-items: center;
    gap: 9rem;
    flex-wrap: wrap;
`

